- return error message along with the number
- ? get exit_status from time to time in portod mail loop
- sleeps & finite number of retries in cgroup kill
- state recovery: kill freezed tasks fails
- freezer wait finite number of times for freezed state
- -v should return git commit hash AND recent git tag (git describe, see http://git.kernel.org/cgit/git/git.git/tree/GIT-VERSION-GEN?id=master)

# error handling
- TError should return rpc::Error enum so we can pass it directly to client
- ? RPC api to get last error message

# cleanup
- rotate subprocess stdout/stderr
- property:env support ; (via \;)
- permissions of .pid, .socket, etc
- move all 'extern "C"' into one file

# debug
- porto debug
- porto debug set container_kill_timeout 100000

# +-
- error handling
- recovery (task recovery)
