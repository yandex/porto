#!/bin/sh
set -e

case "$1" in
    install|upgrade)
        if ! getent group porto >/dev/null; then
            groupadd -g 1333 porto
        else
            echo "porto group already exists"
        fi
    ;;
esac

exit 0
