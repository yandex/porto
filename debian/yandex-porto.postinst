#!/bin/sh
# postinst script

##DEBHELPER##

case "$1" in
    configure)
        chown root:porto /usr/sbin/portoctl || exit 2
    ;;
    upgrade)
	invoke-rc.d yandex-porto reload || exit $?
    ;;
esac

exit 0
