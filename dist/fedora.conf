CONFIG fedora
GET https://github.com/lsm5/docker-brew-fedora/blob/master/fedora-20-medium.tar.xz?raw=true

RUN yum -y update
RUN yum -y install fakeroot debhelper devscripts git cmake gcc gcc-c++ make libstdc++-devel libstdc++-static protobuf-static protobuf-devel protobuf-compiler libnl3-devel

DEBUILD -d

# yum -y --installroot=/home/stfomichev/src/porto/dist/fedora20 --releasever=20 --nogpg install systemd passwd yum fedora-release vim-minimal openssh-server procps-ng iproute net-tools dhclient
# sudo ./portoctl exec fedora20 command="/sbin/init" root=/home/stfomichev/src/porto/dist/fedora20 env="container=lxc"
# sudo ./portoctl enter fedora20
